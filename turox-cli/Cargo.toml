[package]
name = "turox-cli"
version = "0.1.0"
edition = "2021"
# Matches turox-engine's floor (see its Cargo.toml), which this depends on.
rust-version = "1.87"

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
turox-engine = { path = "../turox-engine" }
